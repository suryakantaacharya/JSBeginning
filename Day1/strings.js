// concat 2 strings
console.log("one" + "two");

let name = "surya"
let repoCOunt = "40"

console.log(`hello my name is hello is ${name} and repo count is ${repoCOunt}`)

let regex = "%surya%"

let email = "sssuryaaa"
if(email.match(regex)) {
    console.log(true);
}